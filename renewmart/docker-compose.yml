
services:
  # PostgreSQL Database
  postgres_db:
    image: postgres:15-alpine
    container_name: renewmart-postgres
    environment:
      POSTGRES_DB: renewmart_db
      POSTGRES_USER: renewmart_user
      POSTGRES_PASSWORD: renewmart_password
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ./renewmart/data/postgres:/var/lib/postgresql/data
      - ./backend/renew-sql.sql:/docker-entrypoint-initdb.d/02-renew-sql.sql
    env_file:
      - .env
    ports:
      - "${POSTGRES_HOST_PORT}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U renewmart_user -d renewmart_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - renewmart-network
    restart: unless-stopped

  # Redis for caching and rate limiting
  redis:
    image: redis:7-alpine
    container_name: renewmart-redis
    command: redis-server --appendonly yes
    env_file:
      - .env
    ports:
      - "${REDIS_HOST_PORT}:6379"
    volumes:
      - ./renewmart/data/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - renewmart-network
    restart: unless-stopped

  # Backend API
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: renewmart-backend
    env_file:
      - .env
      - ./backend/.env
    environment:
      - DATABASE_URL=postgresql://renewmart_user:renewmart_password@postgres_db:5432/renewmart_db
      - REDIS_URL=redis://redis:6379/0
      - RENEWMART_REDIS_HOST=redis
      - RENEWMART_REDIS_PORT=6379
      - RENEWMART_REDIS_DB=0
      - RENEWMART_REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=your-secret-key-change-in-production
      - DEBUG=true
    volumes:
      - ./backend:/app
      - ./renewmart/uploads:/app/uploads
      - ./renewmart/logs:/app/logs
    ports:
      - "${BACKEND_HOST_PORT}:1313"
    depends_on:
      postgres_db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:1313/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - renewmart-network
    restart: unless-stopped

  # Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: ../Dockerfile.frontend
    container_name: renewmart-frontend
    env_file:
      - .env
      - ./frontend/.env
    environment:
      - VITE_API_URL=http://backend:1313/api
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "${FRONTEND_HOST_PORT}:1312"
    depends_on:
      - backend
    networks:
      - renewmart-network
    restart: unless-stopped

  # Nginx Reverse Proxy (Optional)
  nginx:
    image: nginx:alpine
    container_name: renewmart-nginx
    env_file:
      - .env
    ports:
      - "${NGINX_HOST_PORT}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - frontend
      - backend
    networks:
      - renewmart-network
    restart: unless-stopped
    

networks:
  renewmart-network:
    driver: bridge
