# Frontend Environment Variables
# Copy this file to .env in the same directory
# File location: renewmart/frontend/.env

# Backend API URL - use environment variable or default
# In Docker, this is set automatically via docker-compose
# For local development, set this to your backend URL
VITE_API_BASE_URL=${VITE_API_BASE_URL:-http://localhost:${BACKEND_HOST_PORT:-1313}/api}
VITE_PORT=${VITE_PORT:-${FRONTEND_HOST_PORT:-1312}}

